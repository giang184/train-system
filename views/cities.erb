<h1>Metro Transit System</h1>

<h3>Metro Transit Cities</h3>
<% if @cities.any? %>
  <ul>
    <% @cities.each do |city| %>
      <li><a href="/cities/<%= city.id %>"><%= city.name %></a></li>
    <% end %>
  </ul>
<% else %>
  <p>There are currently no cities listed.</p>
<% end %>

<p><a href="/cities/new">Add a new city</a></p>